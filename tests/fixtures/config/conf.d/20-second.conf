<?xml version="1.0"?>
<fontconfig>
  <dir>/synthetic/second</dir>
  <!-- Includes its own parent. Real conf.d files do this; it must terminate. -->
  <include ignore_missing="yes">fonts.conf</include>
</fontconfig>
