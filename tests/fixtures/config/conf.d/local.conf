<?xml version="1.0"?>
<!DOCTYPE fontconfig SYSTEM "urn:fontconfig:fonts.dtd">
<!-- Not numerically prefixed, so fontconfig never reads this file.

     It is here to prove that: if this directory is ever loaded wholesale,
     the directory below appears and the test fails. -->
<fontconfig>
  <dir>/synthetic/should-never-load</dir>
</fontconfig>
